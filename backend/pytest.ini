[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = app/tests
markers =
    integration: requires a live Postgres (AXON_DATABASE_URL)
    ollama: requires a reachable Ollama host for real embeddings
