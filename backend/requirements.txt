fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
pgvector>=0.3
redis>=5.0
httpx>=0.27
anthropic>=0.39        # LLM gateway, reason tier (Phase 2)
alembic>=1.13          # migrations, baseline = artifacts/schema.sql (Phase 1)
PyJWT>=2.8             # auth, real impl lifted from carecore/havencore (Phase 1)
faster-whisper>=1.0    # self-hosted STT (CTranslate2 + bundled PyAV decode)
python-multipart>=0.0.9  # UploadFile parsing for /voice/stt
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5             # lint + format (dev)
