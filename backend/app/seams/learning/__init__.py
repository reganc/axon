"""learning seam — mastery model + spaced-repetition scheduler. Phase 2."""


class StubLearning:
    async def record(self, event):
        raise NotImplementedError("learning seam — Phase 2")

    async def update_mastery(self, checkout_id, node_id):
        raise NotImplementedError("learning seam — Phase 2")

    async def due_reviews(self, checkout_id):
        raise NotImplementedError("learning seam — Phase 2")
